import { VrowDirective } from './vrow.directive';

describe('VrowDirective', () => {
  it('should create an instance', () => {
    const directive = new VrowDirective();
    expect(directive).toBeTruthy();
  });
});
